import Navbar from '@components/Navbar';
import Header from '@components/Header';

const Home = () => {
  return (
    <section className="relative">
      <Navbar />
      <Header />
    </section>
  );
};

export default Home;
